# SDM-REACT 基本設計書
## Al–Si–Ti–Fe–Cu五元系の焼結拡散マルチプル法に向けた、反応経路・観測可能性・実験選択の統合設計基盤

| 項目 | 内容 |
|---|---|
| 文書版 | 1.0 |
| 文書状態 | 研究開発用の設計仕様。実装済み・実験検証済みではない |
| 対象材料系 | Al–Si–Ti–Fe–Cu |
| 主対象工程 | 粉末配置、焼結後状態の形成、焼鈍、冷却、組織・組成・相の観測 |
| 主目的 | 五元系への適用条件を解明し、再現可能で解釈可能な材料情報を効率よく取得する |
| 設計の中心 | 到達可能性解析、反応経路の設計、観測上の対立説明の識別 |
| 初期実装方針 | 一つの作製介入について、作製条件から実験選択までを縦断的に実装する |
| 本書で確定しないもの | 実験温度、保持時間、粒径、配合、物性値、必要試料数、具体的装置条件 |

---

# 0. 本設計の要約

本システムは、粉末の濃度分布を描くだけの拡散シミュレータではない。

> **欲しい材料情報を定義し、その情報が得られるための供給・反応・寸法・観測条件を分解し、実際に作製できる粉末配置と熱履歴を選ぶシステムである。**

設計の中心は、次の三つである。

1. **到達可能性**  
   原料量、供給経路、反応、時間、測定能力から、目標情報を取得できる条件を絞る。

2. **反応経路の設計**  
   元素の総量だけでなく、供給位置、接触関係、到着順序、保持・停止時点を設計する。

3. **識別可能性**  
   単相、微細混合、濃度勾配、冷却変態など、同じ観測を説明する異なる材料状態を区別する実験を選ぶ。

ただし、これらを一つの万能物理モデルで実現しようとはしない。

**保存則・適用範囲・出力能力を明確にした複数のモデルを、目的に応じて接続する。**

---

# 1. 研究目的と成功条件

## 1.1 研究目的

本研究の目的は、Al–Si–Ti–Fe–Cuからなる五元系について、以下を達成することである。

### 目的A：五元系への適用条件の解明

- 五元素が反応領域に参加する条件を明らかにする。
- 供給不足、界面抵抗、捕捉相、遮断層、空隙、液相などの影響を区別する。
- 測定・解釈可能な反応領域が形成される条件域を明らかにする。
- 条件の再現性と適用限界を示す。

### 目的B：状態図の概観に寄与する情報の取得

- 相内部組成
- 相共存関係
- 相間分配
- 第五元素操作に対する応答
- 温度・保持時間・冷却履歴に依存した相の変化

を、証拠の強さを区別して蓄積する。

### 目的C：新規材料候補の探索

- 未探索の組成域
- 既知構造の固溶域拡張
- 未帰属相
- 新しい結晶相の候補
- 特徴的な反応経路で形成される材料状態

を抽出し、確認実験につなげる。

---

## 1.2 シミュレーションの役割

シミュレーションの成功を、画像の精細さや校正誤差の小ささだけで評価しない。

最上位の成功条件は、

> **同じ作製・測定予算の下で、単純な経験則や既存条件よりも、有効な試料・測定・追試を選べたこと。**

である。

これを次の四段階に分けて検証する。

| 段階 | 検証する能力 |
|---|---|
| V1 | 保存則・数値計算が正しい |
| V2 | 未使用条件の観測量を予測できる |
| V3 | 作製前に条件比較ができる |
| V4 | 推奨によって材料情報の取得効率が改善する |

V2に成功しても、V3またはV4が成立したとはみなさない。

---

# 2. 基本原則と禁止事項

## 2.1 基本原則

1. 研究上ほしい情報を、計算対象より先に定義する。
2. 実際に作製できる操作だけを設計変数にする。
3. 元素保存と有限供給量を全計算で共有する。
4. 形成状態と観測状態を分ける。
5. 観測への適合と材料状態の一意性を分ける。
6. 不確かさが重要な判断を変える場合、単一の最適解を出さない。
7. 計算の詳細化より、識別実験が有効な場合は実験を選ぶ。
8. 未知物性を都合のよい値で補完しない。
9. モデルの適用範囲内での結論と、実材料に対する結論を区別する。

## 2.2 禁止事項

以下を禁止する。

- 五元素の検出を、五元素単相の証拠とする。
- 点密度ピークを、相または平衡の証拠とする。
- 独立に計算した元素到達指標を規格化し、物理的な組成場として出力する。
- 相モデルなしに相内部組成や相分率を予測する。
- 熱力学的に不安定という理由だけで、準安定候補を不存在とする。
- 試料全体の元素量を、そのまま局所反応領域への供給可能量とする。
- 冷却未評価の室温相を、焼鈍温度での相として扱う。
- 作製後の実測構造を入力して得た予測を、作製前予測として評価する。
- 観測と整合する一つの状態を、唯一の真の状態として採用する。
- 未分類領域を集計分母から除外して成果を大きく見せる。
- 画素数の増加を、独立試料数の増加として扱う。
- 任意のシナリオ重みから精密な成功確率を作る。

---

# 3. 設計範囲

## 3.1 主系列

主系列は、五元素の単体粉末を用いる焼結拡散マルチプル法とする。

設計変数の候補は以下である。

- 全体配合
- 元素別粒径分布
- 粉末の配置・混合状態
- 局所供給源の位置
- 接触関係
- 焼結条件
- 焼鈍温度履歴
- 保持終了時点
- 冷却履歴
- 測定条件
- 測定位置・抽出方法

## 3.2 補助系列

必要に応じて、以下を補助系列として使用できる。

- 制御接触試料
- 層状配置試料
- 局在供給試料
- 原料形態を変更した比較試料
- 予備合金粉末を用いた比較試料
- 観測応答の校正試料

これらは、主系列と同じ方法として無条件に合算しない。

特に予備合金粉末を用いる場合は、**原料形態を変えた比較研究**として区別する。

## 3.3 初期対象外

初期実装では、以下の能力を主張しない。

- 五元系全域の完全な相平衡予測
- 未知結晶相の一般的な生成予測
- 全粉末の全面三次元焼結・反応・流体連成
- 全粒界・酸化膜・空隙の直接追跡
- 任意の温度・配置への無条件な外挿
- 観測されなかった相の不存在証明

---

# 4. 用語・数量・状態変数

## 4.1 材料状態

材料状態を概念的に、

\[
S=
\left\{
\mathcal G,\,
n_{i,a},\,
\mathcal P_a,\,
f_{p,a},\,
\mathcal I,\,
T,\,
H
\right\}
\]

と表す。

- \(\mathcal G\)：領域形状と接続構造
- \(n_{i,a}\)：領域 \(a\) の元素 \(i\) の原子モル数
- \(\mathcal P_a\)：領域内の相または相候補
- \(f_{p,a}\)：相分率
- \(\mathcal I\)：界面状態
- \(T\)：温度
- \(H\)：履歴

全モデルがこれらをすべて予測する必要はない。

未予測・未測定の項目は、欠測または候補集合として保持する。

## 4.2 基本数量

| 記号 | 定義 | 単位 |
|---|---|---|
| \(n_{i,a}\) | 元素の原子モル数 | mol |
| \(V_a\) | 領域体積 | m³ |
| \(c_{i,a}\) | 原子モル濃度 | mol/m³ |
| \(x_{i,a}\) | 原子分率 | 無次元 |
| \(\mathbf J_i\) | 面積当たり元素流束 | mol/(m²·s) |
| \(\Phi_{i,ab}\) | 領域間元素流量 | mol/s |
| \(\mu_i\) | 化学ポテンシャル | J/mol |
| \(\xi\) | 反応層厚などの長さ | m |
| \(T\) | 絶対温度 | K |
| \(t\) | 時間 | s |

原子分率は、

\[
x_{i,a}
=
\frac{n_{i,a}}{\sum_j n_{j,a}}
\]

とする。

複数領域の平均組成は、

\[
\overline{x}_i
=
\frac{\sum_a n_{i,a}}
{\sum_a\sum_j n_{j,a}}
\]

から求める。

異なる相の原子分率を、無条件に体積で単純平均しない。

## 4.3 主五元素と追加成分

酸素などの追加成分を追跡する場合、全追跡成分基準と主五元素基準を分ける。

主五元素に再規格化した組成だけを保存してはならない。

以下を保持する。

- 元の分析値
- 分析合計
- 元素ごとの定量状態
- 主五元素再規格化値
- 追加成分の測定・未測定状態

---

# 5. 目標情報の定義

## 5.1 目標情報の種類

| ID | 目標情報 |
|---|---|
| Q1 | 五元素が定量された反応領域 |
| Q2 | 混合影響を評価できる局所平均組成 |
| Q3 | 同定相の相内部組成 |
| Q4 | 相共存・相間分配 |
| Q5 | 第五元素操作への応答 |
| Q6 | 反応機構または失敗機構の識別 |
| Q7 | 未説明材料状態の候補 |
| Q8 | 候補の独立再現・形成経路の確認 |

一つの目標が複数の種類を持つことはできるが、成果指標は分ける。

## 5.2 目標情報の仕様

各目標は、最低限以下を持つ。

```yaml
target:
  id: null
  information_type: null
  scientific_question: null
  composition_region: null
  temperature_reference: null
  material_state_required: []
  minimum_observation_requirements: []
  required_evidence: []
  competing_interpretations: []
  success_criteria: []
  failure_criteria: []
  inconclusive_conditions: []
```

## 5.3 品質判定

各条件の判定は、

\[
\{\mathrm{pass},\mathrm{fail},\mathrm{unknown}\}
\]

の三値とする。

複数の必須条件を持つ目標については、

- 全必須条件が合格：`pass`
- 一つ以上の必須条件が不合格：`fail`
- 不合格はないが未評価が残る：`unknown`

とする。

判定不能を合格または不合格へ自動変換しない。

---

# 6. システム全体構成

```text
① 目標情報・設備・予算の定義
                ↓
② 支配機構とデータ適用範囲の監査
                ↓
③ 作製可能な候補設計の生成
                ↓
④ 焼結後初期状態の候補集合を構築
                ↓
⑤ 到達可能性の外側評価
                ↓
⑥ 有限供給・反応経路・熱履歴の予測
                ↓
⑦ 冷却・仮想測定
                ↓
⑧ 対立説明と識別可能性の評価
                ↓
⑨ 情報増分・費用・頑健性の比較
                ↓
⑩ 作製／追加測定／識別実験を選択
                ↓
⑪ 実験前予測を固定
                ↓
⑫ 独立検証・モデル更新
```

各処理は、自身に必要な入力だけを要求する。

例えば、物性値が不足していても、既存組成マップの観測品質監査や対立説明の整理は実行できる。

---

# 7. 支配機構の監査

## 7.1 目的

計算前に、どの物理を無視でき、どの物理を残すべきかを決める。

固相拡散を無条件の既定値にはしない。

## 7.2 監査対象

- 固相内拡散
- 粒界などの優先輸送
- 界面反応
- 酸化膜・表面層
- 中間相による捕捉
- 反応層による遮断
- 空隙・剥離
- 液相生成
- 溶解・濡れ・浸透
- 体積変化
- 冷却変態

## 7.3 判定状態

| 状態 | 意味 |
|---|---|
| supported | 関与を支持する観測がある |
| bounded_small | 目的観測量に対する影響が小さいと評価されている |
| unresolved | 関与の可能性が未解決 |
| outside_model | 関与し得るが現在のモデルでは扱えない |

「観測されない」を、`bounded_small`へ自動変換しない。

## 7.4 モデル分岐

重要な機構が未解決で、その有無により条件順位が変わる場合は、以下のいずれかを選ぶ。

1. 複数機構のシナリオを計算する。
2. 適用範囲を狭める。
3. 機構識別実験を優先する。

---

# 8. 作製条件から初期状態への写像

## 8.1 設計変数

作製設計を、

\[
d_{\mathrm{fab}}
=
\{
\text{配合},
\text{粒径分布},
\text{配置},
\text{混合},
\text{圧粉},
\text{焼結履歴}
\}
\]

とする。

ただし、実験で変更できない変数は操作変数に含めない。

例えば、界面移動度を計算上直接変更できても、それを実現する作製操作がなければ設計案として出さない。

## 8.2 初期状態の表現

十分なデータがある場合は、

\[
p(S_0\mid d_{\mathrm{fab}})
\]

を用いる。

分布を正当化できない場合は、

\[
\mathcal S_0(d_{\mathrm{fab}})
\]

という候補集合を用いる。

確率分布と無重みの候補集合を混同しない。

## 8.3 初期状態の取得方法

- 同一試料の非破壊測定
- 同一表面の再観察
- 姉妹試料
- 複数試料からの構造分布推定
- 適用範囲が検証された工程モデル

断面観察が破壊的な場合、焼鈍前後の同一粒子対応を当然視しない。

## 8.4 初期に取得する構造情報

優先順位は次とする。

1. 元素別の残存原料量
2. 主要な接触類型
3. 反応層・先行生成物
4. 供給源間距離
5. 空隙・剥離
6. 接触面積
7. 三次元連結性

二次元像から三次元連結性を一意に決定しない。

## 8.5 二種類の予測を分離する

### 作製前予測

設計条件から初期状態候補を予測し、その後の反応を計算する。

### 作製後条件付き予測

実測した焼結後状態を条件として、その後の反応を計算する。

両者の誤差と成功率は別々に評価する。

---

# 9. 到達可能性解析

## 9.1 目的

詳細計算の前に、目標取得に必要な条件を評価する。

到達可能性解析は、原則として**必要条件による外側評価**である。

必要条件を満たすことは、実現の保証ではない。

## 9.2 設計候補ごとの到達状態集合

モデル \(M\)、パラメータ \(\theta\)、初期状態 \(S_0\)、設計 \(d\) に対して、

\[
S(t)=\mathcal F_M(S_0,d,\theta,t)
\]

とする。

許容する初期状態・パラメータ・モデルの集合に対し、到達し得る状態集合を概念的に、

\[
\mathcal R(d)
=
\left\{
\mathcal F_M(S_0,d,\theta,t)
\right\}
\]

とする。

実際にはこの集合を厳密に求めず、次の二つを区別する。

- 必要条件から得る外側近似
- 具体的計算で得た実現候補

## 9.3 原料量制約

目標状態に必要な元素量を \(n_i^{\mathrm{req}}\)、利用可能量の上限を \(n_i^{\max}\) とすると、

\[
n_i^{\mathrm{req}}\leq n_i^{\max}
\]

は必要条件である。

目標組成や領域寸法が範囲で指定される場合、許容範囲内で必要量を最小化して評価する。

すなわち、目標範囲内の最も有利な状態でも原料量が足りなければ除外できる。

## 9.4 混合の凸包制約に関する注意

原料組成の凸包は、**反応・分配を伴わない単純混合**の平均組成を拘束する。

しかし、多元拡散や相分配を伴う局所組成を、一般に初期原料組成の凸包内へ制限してはならない。

局所的な元素分離や選択輸送によって、単純混合では得られない組成が現れ得るためである。

全体系については、外部流入・流出を含む元素収支を使用する。

## 9.5 輸送量制約

時間区間ごとの輸送可能量を、経路ごとに制約する。

時間離散点 \(k\) において、

\[
n_{i,a}^{k+1}
=
n_{i,a}^{k}
+
\sum_b q_{i,ba}^{k}
-
\sum_b q_{i,ab}^{k}
+
s_{i,a}^{k}
\]

とする。

- \(q_{i,ab}^{k}\)：区間内に \(a\) から \(b\) へ移送される量
- \(s_{i,a}^{k}\)：外部からの正味供給量

必要に応じ、

\[
0\leq q_{i,ab}^{k}\leq Q_{i,ab}^{k,\max}
\]

を課す。

相や複数元素が同じ経路容量を共有する場合は、独立上限だけでなく共通容量制約を加える。

この緩和問題は、上り坂拡散・界面平衡・反応順序などを十分に表現しない場合がある。その場合、**実現可能性ではなく、供給面からの外側評価**として扱う。

## 9.6 上限の根拠

輸送上限は、以下のいずれかによって設定する。

- 検証済み物理モデル
- 適用範囲内の校正データ
- 保守的に設定した上限
- 複数機構を包含する明示的なシナリオ

上限の根拠がない場合、有限値を捏造して不可能判定を行わない。

液相や粒界輸送などの重要経路を見落としている可能性がある場合、実材料についての不可能性を主張しない。

## 9.7 判定区分

| 区分 | 定義 |
|---|---|
| excluded_under_assumptions | 明示した仮定の下で必要条件を満たさない |
| unresolved | 必要条件は満たすが実現経路が未確認 |
| model_witness | 具体的なモデル計算で実現案がある |
| experimentally_supported | 指定範囲で実験確認がある |

不可能判定には必ず、

- 使用した仮定
- 除外に使った制約
- 未包含の機構
- 結論が変わる条件

を添付する。

---

# 10. 有限供給・反応の保存的計算

## 10.1 元素在庫を一元管理する

計算領域は、全体として重複しない所有領域に分ける。

各領域の元素量を、唯一の在庫台帳で管理する。

\[
\frac{dn_{i,a}}{dt}
=
-\sum_b\Phi_{i,ab}
+
S_{i,a}^{\mathrm{external}}
\]

内部輸送では、

\[
\Phi_{i,ab}=-\Phi_{i,ba}
\]

を満たす。

閉鎖系では、

\[
\frac{d}{dt}\sum_a n_{i,a}=0
\]

を満たす。

## 10.2 相反応の扱い

相反応は、元素を生成・消滅させない。

化学量論が固定された相量 \(m_p\) を使用する場合、元素量との関係を、

\[
n_i=\sum_p A_{ip}m_p
\]

で表す。

ここで \(A_{ip}\) の定義と単位を固定する。

固溶体のように相組成が可変の場合は、固定化学量論行列だけでは扱えない。相ごとの元素在庫または内部自由度を持つモデルを使用する。

## 10.3 共通供給源

複数反応領域が同じ原料を消費する場合、各局所モデルへ原料全量を独立に与えない。

共通在庫に対する流出を連成して解く。

時間刻み内の合計流出は、利用可能在庫と整合しなければならない。

負在庫が生じた後にゼロへ切り上げる処理は、標準的な保存法としない。

## 10.4 全体モデルと局所詳細モデル

局所詳細モデルを使用する場合、次のいずれかとする。

### 置換方式

詳細領域内の粗い更新を停止し、詳細モデルの結果で置き換える。

### 境界結合方式

詳細モデルと外部モデルを、共有境界の積分流量で結合する。

いずれも、

- 領域の所有権
- 境界流量
- 元素在庫
- 時間区間

を一致させる。

粗いモデルと詳細モデルの反応を二重に加算しない。

---

# 11. 物理モデルの仕様

## 11.1 モデル水準

| 水準 | 主な目的 | 主な出力 |
|---|---|---|
| M0 | 幾何学的・供給的スクリーニング | 供給困難度、必要条件、ボトルネック |
| M1 | 有効反応予測 | 層厚、消費量、遅延、成長停止 |
| M2 | 指定相内の多元輸送 | 濃度場、相内流束 |
| M3 | 指定相集合の多相反応 | 相量、相組成、界面移動 |
| M4 | 特殊機構 | 液相、浸透、粒界、空隙など |

番号が大きいほど常に優れているわけではない。

目的と識別できるデータに応じて選ぶ。

---

## 11.2 M0：供給スクリーニング

例えば、

\[
\ell_i
\sim
\sqrt{\int D_i^{\mathrm{eff}}(T(t))\,dt}
\]

を輸送尺度の一つとして使用できる。

ただし、これは使用する係数の意味に依存する。

- 原子の自己拡散
- 指定相内の輸送
- 複数界面を含む有効輸送

を混同しない。

M0の出力を、定量組成場や相生成予測へ昇格させない。

---

## 11.3 M1：有効反応モデル

層成長モデルの候補として、

\[
\xi^2-\xi_0^2=K_{\mathrm{eff}}(t-t_0)
\]

または、

\[
t-t_0
=
A(\xi-\xi_0)
+
B(\xi^2-\xi_0^2)
\]

を使用できる。

単位は、

- \(K_{\mathrm{eff}}\)：m²/s
- \(A\)：s/m
- \(B\)：s/m²

とする。

必要事項：

1. 反応開始時点の定義
2. 適用温度・時間・形状
3. 反応生成物の組成または組成範囲
4. 層体積と元素消費の接続
5. 供給枯渇の扱い
6. 接触面積の変化
7. 断面幾何と測定応答
8. 未使用条件での検証

\(K_{\mathrm{eff}}\) を直ちに相互拡散係数と呼ばない。

供給枯渇後に成長式をそのまま延長しない。

また、層厚だけを上限で切って、対応する元素消費を放置してはならない。

---

## 11.4 M2：多元輸送

一般的な元素保存は、

\[
\frac{\partial c_i}{\partial t}
=
-\nabla\cdot\mathbf J_i^{\mathrm{tot}}
\]

とする。

総流束を、

\[
\mathbf J_i^{\mathrm{tot}}
=
c_i\mathbf v+\mathbf j_i
\]

と分ける場合、\(\mathbf v\) の参照系を明示する。

適切な独立成分表示で、

\[
\mathbf j
=
-\mathbf L\nabla\widetilde{\boldsymbol\mu}
\]

を用いることができる。

ただし、以下を明確にする。

- 独立成分と従属成分
- 流束の参照系
- 総モル流束または体積流束の制約
- モル体積
- 相のサイト制約
- 格子移動
- 移動度の単位
- 熱力学因子
- 境界条件

追加成分を追跡する場合、主五元素だけの四自由度表示をそのまま使用しない。

---

## 11.5 簡略ネットワーク輸送

等温、固定されたモル量ノード、総モル流量ゼロなどの仮定が妥当な場合、独立化学ポテンシャル差に対し、

\[
\mathbf q_{ab}
=
\mathbf K_{ab}
\left(
\widetilde{\boldsymbol\mu}_a
-
\widetilde{\boldsymbol\mu}_b
\right)
\]

を使用できる。

\[
\mathbf K_{ab}=\mathbf K_{ab}^{\mathsf T},
\qquad
\mathbf K_{ab}\succeq0
\]

とし、閉鎖等温系で整合する自由エネルギーがある場合、輸送部分の散逸性を確認する。

この形式を使っているだけで、体積変化、格子移動、空孔流束を扱ったことにはならない。

---

## 11.6 M3：移動界面・多相反応

界面法線と跳躍の符号規約を固定し、

\[
[\mathbf J_i^{\mathrm{tot}}\cdot\mathbf n]
=
v_n[c_i]
\]

を満たす。

局所平衡界面と有限反応速度界面は別モデルとする。

相候補、核生成条件、相の抑制設定を記録する。

熱力学計算で安定相が得られることは、その相が実験時間内に生成する保証ではない。

---

## 11.7 M4：特殊機構

液相などが重要な場合でも、全面三次元連成を直ちに採用しない。

目的に応じて、

- 経験的浸透距離モデル
- 溶解量モデル
- 界面抵抗モデル
- 連結性変化モデル

などから開始できる。

重要なのは、対象機構を扱っていないモデルで、その機構が支配的な条件を推奨しないことである。

---

# 12. 供給経路と元素参加時刻

## 12.1 経路解析の目的

次を評価する。

- 目的領域へ寄与する供給源
- 供給を制限する界面
- 原料の競合
- 捕捉相と再供給源
- 配置変更への応答

## 12.2 正味移送と原子の由来を分ける

通常の多元拡散計算から得られる正味流束だけでは、個々の原子がどの原料に由来するかを一意に追跡できない場合がある。

したがって、出力を分ける。

### P1：正味流量経路

保存モデルから得られる領域間の正味元素移送。

### P2：仮想介入感度

特定供給源の位置・量・接触を変更した際の予測差。

### P3：供給源由来

対応するトレーサー輸送などのモデルがある場合に限る。

初期実装ではP1とP2を優先する。

P3を、総流束の単純按分で生成しない。

## 12.3 元素参加時刻

元素 \(i\) の参加時刻を定義する場合、何への参加かを明示する。

例：

- 反応領域の元素量が閾値を超える時刻
- 指定相への取り込み量が閾値を超える時刻
- 観測で定量可能になる時刻

これらは別の量である。

例えば、

\[
t_i^{\mathrm{entry}}
=
\inf
\left\{
t:
n_{i,R}(t)\geq n_i^\ast
\right\}
\]

を使えるが、\(n_i^\ast\) は物理的または観測的根拠に基づいて設定する。

初期から領域内に元素が存在する場合、この時刻は新たな供給の到着時刻を表さない。

## 12.4 同時参加と段階参加

目標ごとに、以下を比較する。

- 複数元素が近い時間帯に参加する配置
- 母相形成後に別元素が参加する配置
- 捕捉相の形成を回避する配置
- 中間相を供給源として利用する配置

同時参加を無条件に最適としない。

---

# 13. 情報取得時間窓

## 13.1 定義

指定した保持終了時刻 \(t\) と、その後の冷却・測定条件に対し、目標情報 \(q\) が合格する時刻集合を、

\[
\mathcal T_q(d,A)
=
\left\{
t:
\operatorname{eligible}_q
\left(
\mathcal H_A[
\mathcal C(
\mathcal F(S_0,d,t)
)]
\right)
=
\mathrm{pass}
\right\}
\]

とする。

- \(\mathcal F\)：焼鈍中の反応
- \(\mathcal C\)：冷却
- \(\mathcal H_A\)：測定応答

時間窓は、複数の非連続区間でもよい。

## 13.2 頑健な時間窓

不確かさシナリオ集合を \(\mathcal U\) とすると、すべての採用シナリオで合格する時刻集合を、

\[
\mathcal T_q^{\mathrm{robust}}
=
\bigcap_{u\in\mathcal U}
\mathcal T_q(d,A;u)
\]

とする。

この集合が空の場合、頑健な合格条件が見つからないことを意味する。

目標そのものが不可能とは限らない。

## 13.3 評価項目

- 時間窓の位置
- 時間窓の幅
- 温度誤差への感度
- 初期構造差への感度
- 冷却条件への感度
- 測定条件への感度

単一の最大値より、再現可能な条件域を優先する場合は、その選好を事前に定義する。

---

# 14. 冷却・観測モデル

## 14.1 冷却の状態区分

| 区分 | 内容 |
|---|---|
| C1 | 目的観測量への影響が小さいと確認されている |
| C2 | 冷却変化を物理モデルで扱う |
| C3 | 冷却条件を含む経験モデルで予測する |
| C4 | 冷却影響が未評価 |

C4でも室温観測の解析はできる。

ただし、高温相の存在や高温相平衡の主張には制限を付ける。

## 14.2 観測式

\[
y
=
\mathcal H_A(S_{\mathrm{RT}})
+
\varepsilon
\]

とする。

必要に応じて、

```text
材料状態
→ 信号発生
→ 検出
→ 定量処理
```

を明示する。

単純な組成の空間平滑化は、有効近似として検証された範囲でのみ用いる。

## 14.3 観測モデルの対象

- 分析体積
- 深さ方向の重なり
- 元素別応答
- 定量限界
- 定量の系統誤差
- 位置合わせ
- 表面・断面状態
- 切断方向
- 相境界の傾き
- 測定点の選択

## 14.4 観測可能性の判定

固定の最小層厚だけで決めない。

以下を組み合わせる。

- 検出可能性
- 組成推定の偏り
- 隣接相混入
- 深さ方向混合
- 不確かさ
- 構造測定との対応
- 測定負担

---

# 15. 対立説明探索

## 15.1 目的

現在の観測を説明できるが、材料学的意味が異なる状態を探索する。

最良適合状態を一つ選ぶことではなく、**主張を一意にできない原因を明らかにすること**を目的とする。

## 15.2 状態集合

観測 \(y\) に整合する状態集合を、

\[
\mathcal S(y)
=
\left\{
S:
D\!\left(y,\mathcal H_A(S)\right)
\leq \tau
\right\}
\]

とする。

- \(D\)：観測誤差と系統誤差を考慮した不一致尺度
- \(\tau\)：校正または事前定義に基づく許容値

\(D\) と \(\tau\) を観測後に都合よく変更しない。

## 15.3 初期の対立説明ライブラリ

- 均一単相
- 相内濃度勾配
- 既知相の微細混合
- 薄層と母相の平均化
- 深さ方向重なり
- 未反応原料
- 酸化物・不純物
- 定量異常
- 位置合わせ誤差
- 冷却変態

このライブラリが全説明を網羅しているとは主張しない。

## 15.4 識別実験の選択

候補行動 \(a\) に対し、仮説 \(h\) の予測観測集合を、

\[
\mathcal Y_h(a)
\]

とする。

仮説間の予測集合が、測定誤差を考慮してどの程度分離するかを評価する。

確率に根拠がある場合は期待情報利得を使用できる。

根拠がない場合は、

- 予測区間の分離
- 最悪条件での分離
- 判定不能となる範囲
- 費用

を比較する。

複数の説明が残る場合、「一意に支持できない」と返す。

---

# 16. 反応配置の設計

## 16.1 候補配置の種類

### R：無作為混合

多様な接触を生成する探索系列。

### P1：距離制御

総量を保ちながら、特定元素の供給距離を変更する。

### P2：接触順序制御

同じ総配合で、どの原料・相が先に接触するかを変える。

### P3：局在供給

一元素を特定領域へ配置し、参加経路を変える。

### P4：多方向供給

反応領域へ異なる方向から元素を供給する。

### P5：複数反応モジュール

一試料内に、異なる設計領域を配置する。

## 16.2 作製可能性制約

候補生成時に、以下を課す。

- 使用可能な原料
- 配置分解能
- 粉末取り扱い可能量
- 混合・凝集の制約
- 圧粉・焼結の制約
- 試料寸法
- 領域間干渉
- 設備の温度・雰囲気・冷却能力
- 測定アクセス

作製誤差を含めると構造が維持できない候補は、理想配置のまま推奨しない。

---

# 17. 操作可能性の評価

## 17.1 局所応答行列

操作変数を \(\mathbf u\)、追跡可能な出力を \(\mathbf z\) として、

\[
\mathbf B
=
\frac{\partial \mathbf z}{\partial\mathbf u}
\]

を考える。

出力には、目的に応じて、

- 独立組成変数
- 層厚
- 相分配指標
- 観測可能面積
- 元素参加時刻

を使用する。

## 17.2 使用条件

感度評価には、比較する出力の対応が必要である。

異なる設計で相や領域そのものが消失する場合、同じ相の組成微分として扱わない。

その場合は、有限介入に対する結果集合を比較する。

## 17.3 解釈

特異値などから、

- 操作がほとんど効かない方向
- 複数操作が同じ結果を生む方向
- 独立した応答を作りやすい方向

を診断する。

単位・操作可能幅・測定誤差を考慮して規格化する。

感度行列の階数が高いことを、五元組成空間全域の被覆証明とはしない。

---

# 18. 材料情報の記録と評価

## 18.1 情報記録

各情報単位は、以下を持つ。

```text
試料・位置
温度と熱履歴
局所平均組成
相内部組成
相または相候補
相共存・分配
組織尺度
観測方法
品質状態
独立性
代替説明
支持できる主張
```

同じ組成でも、相や温度が異なれば別情報となる。

同じ相・組成・温度を繰り返し測定した場合は、主として精度や再現性の増分として評価する。

## 18.2 点密度解析

点密度解析は、候補領域と反応経路の探索に使用する。

必須事項：

- 距離尺度
- 帯域幅
- 境界処理
- ゼロ・定量限界以下の扱い
- 抽出重み
- 実空間への対応
- 相内部候補と混合候補の層別

組成ノイズが増えるだけで、品質付き探索成果が増えないことを試験する。

## 18.3 状態図情報の証拠構造

以下を別の関係として記録する。

- 実空間での隣接
- 同一局所領域での共存
- 相間分配
- 局所平衡との整合性
- 時間安定性
- 異なる初期条件からの再現
- 冷却条件への依存

隣接と平衡共存を同一視しない。

保持時間を変えて変化しないことだけで、平衡と反応停止を区別できるとは限らない。

---

# 19. 試料全体への集計

## 19.1 評価測定と候補探索測定を分ける

### 評価測定

条件間を公平に比較するための測定。

- 共通の評価枠
- 系統抽出または確率抽出
- 共通の測定負担
- 事前定義した品質条件

### 候補探索測定

有望領域を意図的に深く調べる測定。

候補探索測定を、抽出設計なしに試料全体の頻度推定へ混ぜない。

## 19.2 面積割合

品質条件を満たす面積を \(A_q\)、評価枠面積を \(A_W\) とすると、

\[
Y_q=\frac{A_q}{A_W}
\]

とする。

合格・不合格・判定不能を併記する。

二次元面積割合を三次元体積割合へ変換する場合は、切断・抽出の仮定を明示する。

## 19.3 独立性

以下を区別する。

- 原料ロット
- 作製バッチ
- 熱処理バッチ
- 試料
- 断面
- 視野
- 反応領域
- 画素

複数の反応モジュールを同じ試料に置いても、独立作製反復にはならない。

---

# 20. 実験選択規則

## 20.1 行動の種類

- 作製条件変更
- 配置変更
- 熱履歴変更
- 冷却変更
- 焼結後状態の確認
- 追加組成測定
- 構造測定
- 観測校正
- 物性校正
- 詳細計算
- 候補再現
- 仮説検討の停止

## 20.2 目的別評価

単一総合点を既定値にしない。

最低限、以下を別軸で持つ。

\[
\mathbf U(d)
=
\left(
\Delta I_{\mathrm{composition}},
\Delta I_{\mathrm{phase}},
\Delta I_{\mathrm{mechanism}},
R,
-C
\right)
\]

- 組成情報増分
- 相・共存情報増分
- 機構識別情報増分
- 再現性・頑健性
- 費用

各指標の具体的定義は、キャンペーン開始時に固定する。

## 20.3 試料群の補完性

候補は単体評価だけでなく、すでに選んだ試料群に対する追加情報で評価する。

似た経路を繰り返す試料は、単独では有望でも追加価値が小さい場合がある。

一方、独立再現が必要な段階では、重複する条件にも明確な価値がある。

## 20.4 不確かさに対する選択

根拠ある確率分布がある場合：

- 期待成果
- 予測区間
- 失敗確率
- 情報利得

を使用できる。

確率分布に根拠がない場合：

- シナリオ別成果
- 順位安定性
- 優越関係
- 最悪条件
- 最大後悔

を使用する。

シナリオにより順位が逆転する場合、単一推奨を避け、識別実験または代替案を返す。

## 20.5 選択アルゴリズム

```text
1. 目標情報と制約を読み込む
2. 実際に作製可能な候補を生成する
3. 各候補の初期状態候補を構築する
4. 到達可能性の必要条件で候補を絞る
5. 適用可能なモデルで予測する
6. 冷却・仮想測定へ接続する
7. 目標品質と対立説明を評価する
8. 既存情報との増分を計算する
9. 明確に劣る候補を除く
10. 不確かさに対する順位を比較する
11. 改善・識別・探索・確認の役割を付与する
12. 推奨案、代替案、判断不能理由を返す
```

---

# 21. 不確かさ・校正・識別可能性

## 21.1 不確かさの分類

- 作製変動
- 初期状態不確かさ
- 測定誤差
- パラメータ不確かさ
- モデル構造不確かさ
- 適用範囲外への外挿
- 数値誤差

これらを一つのノイズへまとめない。

## 21.2 校正前に固定するもの

- 推定するパラメータ
- 固定するパラメータ
- 使用する観測
- 使用しない検証データ
- 初期状態の扱い
- 境界条件
- 誤差モデル
- パラメータ範囲の根拠

## 21.3 多元輸送の識別

一つの拡散プロファイルから、多元拡散行列全体を一意に推定できるとはしない。

下位系のデータ、異なる勾配方向、制御配置、異なる時間条件などを組み合わせる。

推定できない場合は、

- 有効係数
- 組合せパラメータ
- 予測可能な観測量

に限定して報告する。

## 21.4 モデル不一致

自由度の高い補正項で観測をすべて吸収し、物理モデルを正当化しない。

系統的残差がある場合は、

- 適用範囲縮小
- 競合機構追加
- 初期状態の再評価
- 観測モデルの再評価
- 識別実験

を選ぶ。

---

# 22. ソフトウェア構成

## 22.1 モジュール

| モジュール | 責務 |
|---|---|
| `GoalCompiler` | 研究目標を必要条件・観測条件へ分解する |
| `ProcessState` | 作製条件と初期状態候補を接続する |
| `ScopeAudit` | 支配機構、物性、適用範囲を管理する |
| `Reachability` | 保存量・輸送量・寸法の必要条件を評価する |
| `ReactionSolver` | 有限供給、反応、輸送、局所詳細計算を行う |
| `ObservationSolver` | 冷却と仮想測定を扱う |
| `AlternativeExplorer` | 対立説明を構築し識別性を評価する |
| `EvidenceStore` | 材料情報と主張の証拠を保持する |
| `ExperimentPlanner` | 行動候補の比較と選択を行う |
| `Validation` | 数値検証・独立実験検証を管理する |

初期は単一コードベースとし、分散システムを前提としない。

## 22.2 共通インターフェース

```text
check_requirements(input)
describe_capabilities()
describe_domain()
predict(input)
diagnose(result)
```

各モデルは以下を宣言する。

- 必要入力
- 状態変数
- 保存量
- 単位
- 境界条件
- 予測可能量
- 予測できない量
- 適用範囲
- 不確かさの形式
- 数値診断
- 検証記録

## 22.3 結果状態

結果状態は複数軸で保存する。

### 実行

- `success`
- `partial`
- `insufficient_input`
- `numerical_failure`

### 適用範囲

- `validated`
- `calibrated_only`
- `extrapolated`
- `unsupported`

### 識別

- `identifiable`
- `partially_identifiable`
- `not_identifiable`
- `not_assessed`

### 出力の性質

- `physical_prediction`
- `empirical_prediction`
- `scenario_result`
- `outer_bound`
- `observation_only`

「計算成功」と「科学的に支持された予測」を分ける。

---

# 23. データ設計

## 23.1 主要エンティティ

- `campaign`
- `target`
- `material_lot`
- `design`
- `sample_instance`
- `process_history`
- `initial_state_ensemble`
- `geometry`
- `inventory`
- `interface`
- `model`
- `parameter_set`
- `simulation_run`
- `measurement`
- `sampling_frame`
- `hypothesis`
- `evidence`
- `claim`
- `action`
- `validation_record`

## 23.2 来歴

派生データは上流参照を持つ。

```text
生データ
→ 校正・定量
→ 位置合わせ
→ 領域抽出
→ 構造・組成推定
→ モデル校正
→ 予測
→ 行動選択
```

元データを上書きしない。

コード版、データ版、乱数シード、処理条件を保存する。

## 23.3 行動票

```yaml
action:
  id: null
  target_ids: []
  role: null
  # improvement / discrimination / exploration / confirmation

  modified_controls: []
  fixed_conditions: []
  fabrication_constraints: []

  initial_state_prediction: null
  model_ids: []
  predicted_observations: []
  uncertainty_representation: null

  required_measurements: []
  sampling_plan: null
  success_criteria: []
  rejection_criteria: []
  inconclusive_conditions: []

  competing_hypotheses: []
  next_step_if_supported: null
  next_step_if_rejected: null

  limitations: []
  preregistration_timestamp: null
```

---

# 24. 数値実装方針

## 24.1 保存的離散化

主輸送計算には、有限体積法または保存的領域ネットワークを第一候補とする。

必要事項：

- 内部流量の反対称性
- 元素在庫の非負性
- 境界流量の積分整合
- 時間刻み依存性の評価
- 格子依存性の評価

## 24.2 時間積分

剛性の強い反応や拡散には陰的解法などを検討する。

ただし、陰的であること自体は非負性や保存性を保証しない。

負値が出る場合は、

- 時間刻み縮小
- 非負性を保つ離散化
- 制約付き更新
- 連成解法の見直し

を行う。

## 24.3 粗視化と詳細化

詳細化時には、

- 元素量
- 相量
- 体積
- 境界位置

の受け渡しを検証する。

粗視化時にも、平均組成の見た目だけでなく元素量を保存する。

## 24.4 数値誤差と研究判断

数値許容値は、保存量の規模と判断に必要な精度から設定する。

計算後に都合よく許容値を変更しない。

格子収束と、計算領域を広げた際の境界感度は別試験とする。

---

# 25. 検証・受入試験

## 25.1 数値検証

| ID | 試験 |
|---|---|
| N01 | 閉鎖系元素保存 |
| N02 | 外部流束を含む収支 |
| N03 | 領域間流量の一致 |
| N04 | 非負性 |
| N05 | 原子・質量・体積換算 |
| N06 | 単純拡散の既知解への収束 |
| N07 | 有限供給源の枯渇 |
| N08 | 共通原料の二重消費防止 |
| N09 | 粗密モデル間の保存 |
| N10 | 適用条件下での熱力学的散逸 |
| N11 | 格子・時間刻み依存 |
| N12 | 領域サイズ・境界依存 |

## 25.2 到達可能性試験

- 実現可能な合成例を誤除外しない。
- 原料量不足の例を検出する。
- 輸送経路を追加したとき、外側到達集合が不合理に縮小しない。
- 緩和した制約による実現可能判定を、物理的実現保証として表示しない。
- 上限の根拠がない場合、強い不可能判定を返さない。

## 25.3 観測・対立説明試験

対象：

- 均一単相
- 相内勾配
- 既知二相混合
- 薄層
- 深さ方向重なり
- 未反応原料
- 位置合わせ誤差

確認事項：

- 偽中間組成の発生
- 単相と混合の識別限界
- 測定条件変更の有効性
- 対立説明が残るときに一意認定しないこと
- ライブラリ外の説明を完全に除外したと主張しないこと

## 25.4 統計試験

- 隣接画素分割を独立検証として使わない。
- 画素数増加だけで試料間不確かさが消えない。
- 選択的測定を全体割合として表示しない。
- 未分類を分母から消さない。
- 観測ノイズ増加だけで品質付き成果が増えない。

## 25.5 前向き実験検証

校正に使わなかった、

- 保持時間
- 配置
- 供給距離
- 配合
- 独立作製試料
- 別バッチ

を用いる。

検証対象と校正対象を実験前に固定する。

---

# 26. 段階的実装計画

## Stage 0：研究課題の固定

### 実施内容

- 第一目標を一つ選ぶ。
- 比較する作製介入を一つ選ぶ。
- 利用可能な測定を確認する。
- 成功・失敗・判定不能を定義する。

### 通過条件

何を比較し、何が観測されれば判断できるかが明確である。

---

## Stage 1：観測と初期状態の成立

### 実施内容

- 焼結後状態の取得方式を決める。
- 測定混合と定量限界を評価する。
- 初期構造の候補を作る。
- 主要な競合機構を列挙する。

### 通過条件

比較条件間で、観測値の意味がそろっている。

---

## Stage 2：最小到達可能性・反応計算

### 実施内容

- 共通元素在庫を実装する。
- 原料量制約を実装する。
- 一つの有効反応モデルを実装する。
- 有限供給と観測可能寸法を接続する。
- 時間窓を計算する。

### 通過条件

一つ以上の未使用条件について、測定可能な観測量を予測できる。

---

## Stage 3：識別実験

### 実施内容

- 重要な対立仮説を二つ以上残す。
- 予測が分かれる配置・時間・測定を選ぶ。
- 実験前予測を固定する。
- 独立実験を行う。

### 通過条件

少なくとも一つの重要な曖昧さを減らせる、または識別不能を正しく示せる。

---

## Stage 4：反応配置の逆設計

### 実施内容

- 作製可能な配置候補を生成する。
- 到達可能性で候補を絞る。
- 時間窓と頑健性を比較する。
- 条件推奨を行う。

### 通過条件

作製後実測を使わない条件比較の有効性を評価できる。

---

## Stage 5：試料群と新規候補探索

### 実施内容

- 無作為混合と制御配置を組み合わせる。
- 異なる組成方向・反応経路を補完する。
- 未説明領域を抽出する。
- 候補再現と構造確認を行う。

### 通過条件

同じ予算の基準手法に対し、材料情報の増分を評価できる。

---

# 27. 第一版の具体的な研究課題

第一版では、次の問いを推奨する。

> **総配合を保ったまま供給配置を変更したとき、測定可能な五元素反応領域の形成時期・寸法・再現性はどう変わるか。また、その変化は供給距離、界面抵抗、先行反応のどれで説明されるか。**

この問いを採用する理由は以下である。

- 現在の五元素粉末研究から大きく逸脱しない。
- 濃度場の再現だけでなく、作製介入の評価になる。
- 有限供給と反応経路が重要になる。
- 単純な経験則との比較ができる。
- 相情報が不完全でも、一部の観測量から開始できる。
- 後に相分配・新規候補探索へ拡張できる。

## 27.1 初期仮説

- H1：供給距離が主要因
- H2：接触・界面抵抗が主要因
- H3：先行反応相による捕捉・遮断が主要因
- H4：見かけの差の一部が観測混合による

仮説を無理に一つへ決めて開始しない。

## 27.2 最低限の比較役割

- 基準配置
- 変更配置
- 焼結後確認
- 時間依存確認
- 独立作製反復
- 未使用条件の検証
- 必要に応じた観測校正

具体的な試料数は、予備データの変動と識別したい差から決める。

---

# 28. 停止・方向転換条件

| 状況 | 対応 |
|---|---|
| 初期構造の不確かさが支配する | 焼結後観測・工程モデルを優先 |
| 詳細化しても条件順位が変わらない | 詳細化停止 |
| 物性が識別できない | 有効量へ縮約または識別実験 |
| 液相などの未包含機構が支配する | 現モデルの主張停止、別モデル系列へ |
| 時間窓が測定・作製誤差より狭い | 頑健な配置へ変更、または目標を見直す |
| 対立説明が解消しない | 主張を弱め、追加測定を比較 |
| 配置制御の負担が成果に見合わない | 無作為混合を主系列に戻す |
| 経験モデルと選択性能が同等 | 物理モデルの負担を縮小 |
| 候補が再現しない | 経路再現と組成再現を分けて調べる |

モデル対象外は、研究対象外を意味しない。

---

# 29. 最終成果物

## 29.1 適用条件地図

- 形成可能域
- 測定可能域
- 解釈可能域
- 未評価域
- モデル対象外域

## 29.2 反応経路地図

- 主供給源
- 主要経路
- ボトルネック
- 捕捉・遮断
- 元素参加の時間関係
- 配置変更への応答

## 29.3 情報取得時間窓

- 目標ごとの時間窓
- 頑健な時間範囲
- 温度・初期構造・冷却への感度

## 29.4 実験処方

- 配合・配置
- 熱履歴
- 測定計画
- 予測
- 不確かさ
- 判定基準
- 代替案

## 29.5 状態図情報・候補台帳

- 相内部組成
- 共存・分配
- 温度・履歴
- 証拠水準
- 対立説明
- 再現状況
- 必要な追試

## 29.6 システム有効性報告

- 経験則との比較
- 経験モデルとの比較
- 作製前予測の性能
- 推奨による情報増分
- 費用と研究負担
- 適用範囲

---

# 30. 本設計の最終方針

本設計で最初に完成させるべきものは、巨大な三次元組織シミュレータではない。

> **一つの作製介入について、有限供給と反応経路を予測し、冷却・測定を通して取得可能な情報へ変換し、未使用条件で検証する小さな一連の系である。**

その核が成立した後に、

- 配置の逆設計
- 元素参加時刻の調整
- 情報取得時間窓
- 多方向の組成操作
- 対立説明を壊す測定設計
- 新規候補の確認

へ拡張する。

最も重要な設計原理は、次の三点である。

1. **形成できることと、測定・解釈できることを同時に設計する。**
2. **濃度の結果だけでなく、供給経路と反応順序を操作する。**
3. **成功案だけでなく、不可能性・未解決性・対立説明を出力する。**

これにより、焼結拡散マルチプル法を、単に多数の組成を偶発的に得る方法ではなく、**必要な五元系材料情報を取得するために反応経路を設計できる探索手法**へ発展させることを目指す。
